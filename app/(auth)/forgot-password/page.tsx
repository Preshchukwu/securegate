import { AuthCard } from "@/components/auth/AuthCard";

export const metadata = { title: "Forgot password — SecureGate" };

export default function ForgotPasswordPage() {
  return <AuthCard initialView="forgot" />;
}
