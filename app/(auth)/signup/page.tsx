import { AuthCard } from "@/components/auth/AuthCard";

export const metadata = { title: "Create account — SecureGate" };

export default function SignUpPage() {
  return <AuthCard initialView="signup" />;
}
